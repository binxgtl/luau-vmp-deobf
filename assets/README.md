# Assets

Repository-owned static assets used by the project README.

`donate-mbbank.svg` is a vector VietQR generated from the exact QR payload supplied by the maintainer for MBBank donations. Keeping the QR in-repository avoids depending on temporary image-hosting URLs.
